// SPDX-License-Identifier: Apache-2.0
//
// Forked from the aurae project's `auraed/src/cri/streaming.rs`
// (https://github.com/aurae-runtime/aurae, `cri` branch, Apache-2.0,
// Copyright 2022-2024 the aurae contributors), with the transport
// genericized over AsyncRead/AsyncWrite.

//! SPDY/3.1 framing: just enough to serve the Kubernetes streaming
//! protocols — frame encode/decode, the zlib header (de)compression with the
//! fixed SPDY dictionary, and the HTTP/1.1 Upgrade handshake.

use flate2::{Compress, Compression, Decompress, FlushCompress, FlushDecompress};
use tokio::io::{AsyncRead, AsyncReadExt, AsyncWrite, AsyncWriteExt};

/// The SPDY/3 header-compression dictionary. Both peers initialise their
/// zlib contexts with this dictionary; it is byte-for-byte the dictionary
/// used by `github.com/moby/spdystream`, which the Kubernetes apimachinery
/// SPDY client is built on, so the two interoperate.
pub(crate) static SPDY_DICTIONARY: &[u8] = &[
    ,
];

// SPDY/3 frame constants.
const SPDY_CONTROL_BIT: u8 = 0x80;
const SPDY_FLAG_FIN: u8 = 0x01;
const TYPE_SYN_STREAM: u16 = 1;
const TYPE_SYN_REPLY: u16 = 2;
const TYPE_RST_STREAM: u16 = 3;
const TYPE_PING: u16 = 6;
const TYPE_GOAWAY: u16 = 7;

/// The largest chunk of container output written in a single SPDY data
/// frame. Comfortably below the default 64 KiB flow-control window, so the
/// tiny payloads the conformance suite moves never need a `WINDOW_UPDATE`.
pub(crate) const MAX_DATA_CHUNK: usize = 16 * 1024;

/// The frames this server reacts to; everything else is consumed and
/// ignored.
pub(crate) enum Frame {
    SynStream {
        stream_id: u32,
        headers: Vec<u8>,
    },
    Data {
        stream_id: u32,
        fin: bool,
        payload: Vec<u8>,
    },
    RstStream {
        stream_id: u32,
    },
    Ping {
        id: u32,
    },
    GoAway,
    Other,
}

fn u24(bytes: &[u8]) -> usize {
    ((bytes[0] as usize) << 16) | ((bytes[1] as usize) << 8) | bytes[2] as usize
}

pub(crate) async fn read_frame<R: AsyncRead + Unpin>(
    read: &mut R,
) -> std::io::Result<Option<Frame>> {
    let mut head = [0u8; 8];
    match read.read_exact(&mut head).await {
        Ok(_) => {}
        Err(e) if e.kind() == std::io::ErrorKind::UnexpectedEof => {
            return Ok(None);
        }
        Err(e) => return Err(e),
    }

    if head[0] & SPDY_CONTROL_BIT != 0 {
        let frame_type = u16::from_be_bytes([head[2], head[3]]);
        let len = u24(&head[5..8]);
        let mut payload = vec![0u8; len];
        let _ = read.read_exact(&mut payload).await?;
        let frame = match frame_type {
            TYPE_SYN_STREAM if payload.len() >= 10 => {
                let stream_id =
                    u32::from_be_bytes([payload[0], payload[1], payload[2], payload[3]])
                        & 0x7fff_ffff;
                Frame::SynStream {
                    stream_id,
                    headers: payload[10..].to_vec(),
                }
            }
            TYPE_RST_STREAM if payload.len() >= 4 => {
                let stream_id =
                    u32::from_be_bytes([payload[0], payload[1], payload[2], payload[3]])
                        & 0x7fff_ffff;
                Frame::RstStream { stream_id }
            }
            TYPE_PING if payload.len() >= 4 => {
                let id = u32::from_be_bytes([payload[0], payload[1], payload[2], payload[3]]);
                Frame::Ping { id }
            }
            TYPE_GOAWAY => Frame::GoAway,
            _ => Frame::Other,
        };
        Ok(Some(frame))
    } else {
        let stream_id = u32::from_be_bytes([head[0] & 0x7f, head[1], head[2], head[3]]);
        let fin = head[4] & SPDY_FLAG_FIN != 0;
        let len = u24(&head[5..8]);
        let mut payload = vec![0u8; len];
        let _ = read.read_exact(&mut payload).await?;
        Ok(Some(Frame::Data {
            stream_id,
            fin,
            payload,
        }))
    }
}

/// The write side of a SPDY connection: owns the socket and the single
/// zlib deflate context shared by every outgoing header block. Wrapped in a
/// mutex so the concurrent output pumps serialise their frames.
pub(crate) struct SpdyWriter<W> {
    write: W,
    compress: Compress,
}

impl<W: AsyncWrite + Unpin> SpdyWriter<W> {
    pub(crate) fn new(write: W) -> Self {
        let mut compress = Compress::new(Compression::default(), true);
        // The dictionary must be installed before the first byte is produced.
        let _ = compress.set_dictionary(SPDY_DICTIONARY);
        SpdyWriter { write, compress }
    }

    /// Compress a name/value header block, continuing the connection's shared
    /// deflate stream (flushed per block so the peer can inflate it alone).
    pub(crate) fn compress_headers(&mut self, pairs: &[(&str, &str)]) -> Vec<u8> {
        let mut raw = Vec::new();
        raw.extend_from_slice(&(pairs.len() as u32).to_be_bytes());
        for (name, value) in pairs {
            raw.extend_from_slice(&(name.len() as u32).to_be_bytes());
            raw.extend_from_slice(name.as_bytes());
            raw.extend_from_slice(&(value.len() as u32).to_be_bytes());
            raw.extend_from_slice(value.as_bytes());
        }

        let mut out = Vec::with_capacity(raw.len() + 128);
        let mut input: &[u8] = &raw;
        loop {
            out.reserve(128);
            let before_in = self.compress.total_in();
            let before_out = self.compress.total_out();
            let _ = self
                .compress
                .compress_vec(input, &mut out, FlushCompress::Sync);
            let consumed = (self.compress.total_in() - before_in) as usize;
            input = &input[consumed..];
            if input.is_empty() && self.compress.total_out() == before_out {
                break;
            }
        }
        out
    }

    async fn write_control(
        &mut self,
        frame_type: u16,
        flags: u8,
        payload: &[u8],
    ) -> std::io::Result<()> {
        let len = payload.len();
        let mut head = [0u8; 8];
        head[0] = SPDY_CONTROL_BIT; // version high byte (3 -> 0x00,0x03)
        head[1] = 0x03;
        head[2..4].copy_from_slice(&frame_type.to_be_bytes());
        head[4] = flags;
        head[5] = (len >> 16) as u8;
        head[6] = (len >> 8) as u8;
        head[7] = len as u8;
        self.write.write_all(&head).await?;
        self.write.write_all(payload).await?;
        self.write.flush().await
    }

    pub(crate) async fn syn_reply(&mut self, stream_id: u32) -> std::io::Result<()> {
        let headers = self.compress_headers(&[]);
        let mut payload = Vec::with_capacity(4 + headers.len());
        payload.extend_from_slice(&(stream_id & 0x7fff_ffff).to_be_bytes());
        payload.extend_from_slice(&headers);
        self.write_control(TYPE_SYN_REPLY, 0, &payload).await
    }

    pub(crate) async fn data(
        &mut self,
        stream_id: u32,
        fin: bool,
        payload: &[u8],
    ) -> std::io::Result<()> {
        let len = payload.len();
        let mut head = [0u8; 8];
        head[0..4].copy_from_slice(&(stream_id & 0x7fff_ffff).to_be_bytes());
        head[4] = if fin { SPDY_FLAG_FIN } else { 0 };
        head[5] = (len >> 16) as u8;
        head[6] = (len >> 8) as u8;
        head[7] = len as u8;
        self.write.write_all(&head).await?;
        if !payload.is_empty() {
            self.write.write_all(payload).await?;
        }
        self.write.flush().await
    }

    pub(crate) async fn rst_stream(&mut self, stream_id: u32, status: u32) -> std::io::Result<()> {
        let mut payload = [0u8; 8];
        payload[0..4].copy_from_slice(&(stream_id & 0x7fff_ffff).to_be_bytes());
        payload[4..8].copy_from_slice(&status.to_be_bytes());
        self.write_control(TYPE_RST_STREAM, 0, &payload).await
    }

    pub(crate) async fn ping(&mut self, id: u32) -> std::io::Result<()> {
        self.write_control(TYPE_PING, 0, &id.to_be_bytes()).await
    }

    pub(crate) async fn goaway(&mut self) -> std::io::Result<()> {
        self.write_control(TYPE_GOAWAY, 0, &[0u8; 8]).await
    }
}

/// The connection's inflate context (also shared across every incoming
/// header block).
pub(crate) struct HeaderInflater {
    decompress: Decompress,
}

impl HeaderInflater {
    pub(crate) fn new() -> Self {
        HeaderInflater {
            decompress: Decompress::new(true),
        }
    }

    pub(crate) fn inflate(&mut self, input: &[u8]) -> Result<Vec<(String, String)>, String> {
        let raw = self.inflate_raw(input)?;
        parse_name_value(&raw)
    }

    fn inflate_raw(&mut self, mut input: &[u8]) -> Result<Vec<u8>, String> {
        let mut out = Vec::with_capacity(input.len() * 6 + 128);
        while !input.is_empty() {
            out.reserve(1024);
            let before_in = self.decompress.total_in();
            match self
                .decompress
                .decompress_vec(input, &mut out, FlushDecompress::Sync)
            {
                Ok(_) => {
                    let consumed = (self.decompress.total_in() - before_in) as usize;
                    input = &input[consumed..];
                    if consumed == 0 {
                        break;
                    }
                }
                Err(e) => {
                    if e.needs_dictionary().is_some() {
                        self.decompress
                            .set_dictionary(SPDY_DICTIONARY)
                            .map_err(|e| format!("set dictionary: {e}"))?;
                        let consumed = (self.decompress.total_in() - before_in) as usize;
                        input = &input[consumed..];
                        continue;
                    }
                    return Err(format!("inflate: {e}"));
                }
            }
        }
        Ok(out)
    }
}

/// Parse a SPDY/3 name/value header block: a 4-byte count followed by that
/// many `(len, name, len, value)` records. Names are lowercased for
/// case-insensitive lookup.
fn parse_name_value(raw: &[u8]) -> Result<Vec<(String, String)>, String> {
    if raw.len() < 4 {
        return Ok(Vec::new());
    }
    let count = u32::from_be_bytes([raw[0], raw[1], raw[2], raw[3]]) as usize;
    let mut cursor = 4;
    let mut pairs = Vec::with_capacity(count);
    let read_field = |raw: &[u8], cursor: &mut usize| -> Option<Vec<u8>> {
        if *cursor + 4 > raw.len() {
            return None;
        }
        let len = u32::from_be_bytes([
            raw[*cursor],
            raw[*cursor + 1],
            raw[*cursor + 2],
            raw[*cursor + 3],
        ]) as usize;
        *cursor += 4;
        if *cursor + len > raw.len() {
            return None;
        }
        let field = raw[*cursor..*cursor + len].to_vec();
        *cursor += len;
        Some(field)
    };
    for _ in 0..count {
        let Some(name) = read_field(raw, &mut cursor) else {
            break;
        };
        let Some(value) = read_field(raw, &mut cursor) else {
            break;
        };
        pairs.push((
            String::from_utf8_lossy(&name).to_ascii_lowercase(),
            String::from_utf8_lossy(&value).into_owned(),
        ));
    }
    Ok(pairs)
}

pub(crate) fn header<'a>(pairs: &'a [(String, String)], name: &str) -> Option<&'a str> {
    pairs
        .iter()
        .find(|(k, _)| k == name)
        .map(|(_, v)| v.as_str())
}

#[cfg(test)]
mod tests {
    use super::*;

    /// A deflate context seeded with the SPDY dictionary can round-trip a
    /// header block through the matching inflate context — the interop
    /// contract with moby/spdystream.
    #[test]
    fn header_block_round_trip_with_dictionary() {
        let (_, mut writer) = tokio::io::duplex(1024);
        let mut spdy = SpdyWriter::new(&mut writer);
        let compressed = spdy.compress_headers(&[("streamtype", "stdout"), ("port", "8080")]);
        assert!(!compressed.is_empty());

        let mut inflater = HeaderInflater::new();
        let pairs = inflater.inflate(&compressed).expect("inflate");
        assert_eq!(header(&pairs, "streamtype"), Some("stdout"));
        assert_eq!(header(&pairs, "port"), Some("8080"));

        // A second block continues the shared deflate stream.
        let compressed2 = spdy.compress_headers(&[("streamtype", "error")]);
        let pairs2 = inflater.inflate(&compressed2).expect("inflate 2");
        assert_eq!(header(&pairs2, "streamtype"), Some("error"));
    }

    #[tokio::test]
    async fn data_and_control_frames_round_trip() {
        let (mut client, server) = tokio::io::duplex(64 * 1024);
        let mut writer = SpdyWriter::new(server);

        writer.data(3, false, b"hello").await.unwrap();
        writer.data(3, true, b"").await.unwrap();
        writer.ping(7).await.unwrap();
        writer.rst_stream(5, 1).await.unwrap();
        writer.goaway().await.unwrap();

        match read_frame(&mut client).await.unwrap().unwrap() {
            Frame::Data {
                stream_id,
                fin,
                payload,
            } => {
                assert_eq!(stream_id, 3);
                assert!(!fin);
                assert_eq!(payload, b"hello");
            }
            _ => panic!("expected data frame"),
        }
        match read_frame(&mut client).await.unwrap().unwrap() {
            Frame::Data {
                stream_id,
                fin,
                payload,
            } => {
                assert_eq!(stream_id, 3);
                assert!(fin);
                assert!(payload.is_empty());
            }
            _ => panic!("expected FIN data frame"),
        }
        match read_frame(&mut client).await.unwrap().unwrap() {
            Frame::Ping { id } => assert_eq!(id, 7),
            _ => panic!("expected ping"),
        }
        match read_frame(&mut client).await.unwrap().unwrap() {
            Frame::RstStream { stream_id } => assert_eq!(stream_id, 5),
            _ => panic!("expected rst_stream"),
        }
        match read_frame(&mut client).await.unwrap().unwrap() {
            Frame::GoAway => {}
            _ => panic!("expected goaway"),
        }
    }

    #[tokio::test]
    async fn syn_reply_frame_parses_as_other_end_control() {
        // SYN_REPLY isn't a frame the *server* reader handles (clients send
        // SYN_STREAM), so it must come back as Other — consumed, not fatal.
        let (mut client, server) = tokio::io::duplex(1024);
        let mut writer = SpdyWriter::new(server);
        writer.syn_reply(1).await.unwrap();
        match read_frame(&mut client).await.unwrap().unwrap() {
            Frame::Other => {}
            _ => panic!("expected Other for SYN_REPLY"),
        }
    }

    #[test]
    fn parse_name_value_handles_truncation() {
        assert!(parse_name_value(&[]).unwrap().is_empty());
        // Claims 5 pairs but carries one: parse what's there, don't panic.
        let (_, mut w) = tokio::io::duplex(64);
        let mut spdy = SpdyWriter::new(&mut w);
        let block = spdy.compress_headers(&[("a", "b")]);
        let mut inflater = HeaderInflater::new();
        let pairs = inflater.inflate(&block).unwrap();
        assert_eq!(pairs, vec![("a".to_string(), "b".to_string())]);
    }
}
